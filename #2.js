/**
 * Completare il codice JavaScript successivo in modo
 * che la funzione "sum" restituisca la somma dei suoi due argomenti
*/

function sum(a, b) {
    let j=a+b;
    return(j)
}

const x = sum(3, 5);
console.log(x) // deve stampare il numero 8
